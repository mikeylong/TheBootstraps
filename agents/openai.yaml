interface:
  display_name: "Bootstrap AI-Native Project"
  short_description: "Create or align agent-ready project context"
  default_prompt: "Use $bootstrap-ai-native-project to create a new agent-ready scaffold or safely align an existing repository."
